spring:

  datasource:
    url: jdbc:mysql://localhost:3306/myfinacial?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 12qwaszx

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: false
    open-in-view: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: none


  mail:
    host: smtp.gmail.com
    port: 587
    username: my.financial.v2
    password: 123@mudar
    properties:
      mail:
        smtp:
          auth: true
          starttls:
          enable: true


  flyway:
    locations: classpath:/db/dev-migration


email:
  app_link_access: http://localhost:8080/login

